{
    "configurations": [
        {
            "type": "node",
            "name": "launch Program",
            "request": "launch",
            "program": "${workspaceFolder}/server.js"
        }
    ]
}